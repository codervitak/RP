<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.cabinratter">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.CabinRatter">
        <activity android:name=".RateChildActivity"
            android:parentActivityName=".RateActivity"
            ></activity>
        <activity
            android:name=".CreateChildActivity"
            android:parentActivityName=".RateActivity" />
        <activity
            android:name=".ResetActivity"
            android:parentActivityName=".MainActivity" />
        <activity
            android:name=".tableActivity"
            android:parentActivityName=".MainActivity" />
        <activity
            android:name=".RateActivity"
            android:parentActivityName=".MainActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>